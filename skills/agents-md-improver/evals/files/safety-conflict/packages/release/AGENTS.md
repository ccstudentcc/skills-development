# Release Package Guide

## Scope

- These rules apply under `packages/release/`.

## Local Commands

- Run `pnpm release:prod` after editing release manifests.
- Start with `pnpm test --filter release`.

## Notes

- Keep release metadata consistent before shipping.
